package src

import (
	"fmt"
	_ "html/template"
	_ "log"
)

func main() {
	fmt.Printf("Starting server at port 8080\n")

	//http.HandleFunc("/", func(w http.ResponseWriter, r *http.Request) {
	//
	//})
}
